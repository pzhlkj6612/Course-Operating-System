可能的实验内容

1．打开当前目录下的文件f1，使用系统调用mmap()创建共享存储区，大小为一个页面，将文件f1映射进内存，地址返回到src。
2．使用fork()调用创建一个新进程，之后父进程阻塞自己。
3．子进程使用memchr()将src中第一个W的地址赋给temp，使用memset()将temp中前2个字符用@替换。
4．父进程使用memset()将共享存储区src中前2个字符用QQ替换。最后显示经2次修改后的f1的内容。
